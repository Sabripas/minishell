To do list

- waiting prompte
- history
- echo
- cd
- pwd
- export
- unset
- env
- exit
- ' '
- '' ''
- <
- >
- >>
- <<
- |
- ctrl-C
- ctrl-D
- ctrl-\
- PATH
- environement variable
- $?
______________

programme recept

0. wait for input

1. use read_line family to make history (Waiting prompt / history)
	read_line to catch input
	make hitory to save input
	replace line to cath save if arow up/down used

2. Split input in token
	use split fonction to split in tokens
	ad atom valu and left/right rule

3. Treat tokens with atom algo (< / > / >> / << / | / '' / "")
	check every token and fine the one with bigger atom value and execute 
	go to 
	atom value - 1 if left
	or value if right



4. Treat wildcard + env variable (environement variable)
	check for * or ? 
	replace ?... by env varianle in path



5. execute token in order
	if built in exucute right code (echo / cd / pwd / export / unset / env / exit / $?) 
	else look PATH to exe right fonction (PATH) 
	order find by atom algo

6 . go to 0

ALL. Look for signal (ctrl-C / ctrl-D / Ctrl-\)
